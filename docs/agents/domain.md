# Domain Docs Layout

This repo uses a **single-context** domain layout:

- Global domain glossary: `CONTEXT.md` (repo root)
- Architectural Decision Records: `docs/adr/` (repo root)

All skills modifying or reading domain terminology look at `CONTEXT.md` at the repo root.
